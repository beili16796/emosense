[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "emokit"
version = "0.1.0.dev0"
description = "EmoKit stub for local development of EmoSense"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "torch>=2.0", "scipy>=1.10"]
